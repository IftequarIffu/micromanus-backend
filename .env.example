# Server
PORT=3000

# Supabase (server only)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
# Optional: personal access token for Management API schema apply (not used at runtime)
# SUPABASE_ACCESS_TOKEN=

# LLM providers (server only)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_GEMINI_API_KEY=

# Search (server only)
TAVILY_API_KEY=

# Stripe (server only)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Encrypt api_keys.encrypted_key at rest (server only)
ENCRYPTION_KEY=

# Protects internal/admin routes if added later (server only)
ADMIN_SECRET=
